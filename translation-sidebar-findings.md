# Translation manager sidebar verification

أضيف مفتاح تنقل `languages` إلى إدارة المنصة فقط، ويرتبط مباشرة بـ `TranslationReviewPanel` داخل المحتوى الأوسط. الأدوار التشغيلية لا ترى القسم بسبب قائمة الصلاحيات في `roleNavigation`.

حالة مجموعات الشريط تحفظ في localStorage بالمفتاح `nfood:sidebar-groups:<managerId>`، وتُستعاد لكل مدير بشكل مستقل ثم تُحدّث عند الطي أو الفتح. المعاينة على سطح المكتب والجوال لم تظهر قصًا في المحتوى الرئيسي. فحص TypeScript، الاختبارات المستهدفة، والبناء نجحت. تعذر جزء من الاختبارات الكاملة مرتين بسبب `Connection lost` من MySQL، وليس بسبب فشل في الكود؛ 60 من 62 ملف اختبار و265 من 267 اختبارًا نجحت في المحاولة الثانية.

# Reference card layout verification — 2026-09-05

The mobile full-page preview for `/menu/nssercafa` was captured after the CSS update. The menu reaches the product sections below the cover and renders the product grid in two columns; the bottom navigation remains visible. The reference-card CSS enforces horizontal card internals with a side image, a text column, compact summary, and a 2rem plus button at mobile widths. TypeScript and production build succeeded; the build retains the pre-existing dashboard-overview chunk warning.

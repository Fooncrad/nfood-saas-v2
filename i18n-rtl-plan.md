# خطة تنفيذ دعم i18n واتجاه RTL/LTR في NFOOD

## الهدف
توحيد دعم العربية والإنجليزية في منصة NFOOD عبر طبقة i18n مركزية قابلة للتوسع، بحيث يتبدل النص واتجاه الواجهة فورًا من دون إعادة تحميل كاملة، ويُحدّث `document.lang` و`document.dir` تلقائيًا، مع الحفاظ على توافق المنيو العام ولوحة الإدارة ونوافذ الحوار والقوائم الجانبية.

## الوضع الحالي الذي ستُبنى عليه الخطة
المشروع يحتوي بالفعل على `LanguageContext` يدير اللغات، مفاتيح التخزين، `languageMeta`، الدالة `t()`، تنسيق الأرقام والتواريخ، وتحديث `document.documentElement.lang/dir` و`document.body.dir`. كما يحتوي على `LanguageSwitcher` يدعم اللغات المنشورة ومؤشر تحميل، وعلى `DatabaseTranslationBridge` وقاموس ترجمة احتياطي. لذلك لن تُنشأ طبقة ثانية موازية؛ سيتم تطوير هذه البنية بدل إدخال مكتبة i18n جديدة قد تضاعف مصادر الحقيقة.

## مراحل التنفيذ

### 1. تثبيت عقد اللغة والاتجاه
سيتم اعتماد نوع لغة واضح للعربية والإنجليزية، مع إبقاء الفرنسية واللغات المنشورة الحالية متوافقة حيثما كانت مستخدمة. ستصبح خريطة اللغة موحدة بين `LanguageContext` و`LanguageSwitcher` وملفات الصفحات، وتتضمن لكل لغة الاسم الأصلي، الاسم المعروض، الاتجاه، والـ locale. سيستمر حفظ اختيار المستخدم في `localStorage` بالمفتاح المناسب للمنيو أو لوحة الإدارة، مع أولوية `?lang=` عند وجوده.

سيتم جعل تحديث `document.documentElement.lang` و`document.documentElement.dir` و`body.dir` جزءًا من تأثير واحد موثوق، مع منع أي إعادة تحميل كاملة. سيبقى مؤشر التحميل الحالي، وستضاف حالات وصول واضحة مثل `aria-busy` و`aria-live` أثناء الانتقال، مع احترام `prefers-reduced-motion`.

### 2. تحويل القاموس إلى ملفات لغة منظمة
سيتم فصل النصوص الأساسية إلى ملفات بيانات مستقلة مثل `client/src/locales/ar.json` و`client/src/locales/en.json`، مع ملف أنواع أو قاموس مفاتيح مشترك يضمن أن مفاتيح اللغتين متطابقة. لن تُخلط العربية والإنجليزية داخل قيمة واحدة؛ ستستخدم المفاتيح الدلالية مثل `auth.signIn` و`menu.addToCart` و`navigation.open` بدل تمرير نصوص عربية عشوائية إلى `t()`.

سيُحافظ على قاموس قاعدة البيانات للنصوص الديناميكية، لكن ستُحدد أولوية الاسترجاع بوضوح: ترجمة قاعدة البيانات للغة المطلوبة، ثم ملف اللغة، ثم قيمة آمنة للمفتاح نفسه. في الواجهات غير العربية لن يُعرض نص عربي خام كحل صامت؛ سيظهر نص بديل مفهوم أو علامة ترجمة ناقصة في لوحة الإدارة، مع عدم كسر واجهة العميل.

### 3. إنشاء مساعد ترجمة عالمي آمن
سيتم تثبيت واجهة `t(key, variables?)` على مستوى السياق، مع دعم الاستبدال الآمن للمتغيرات مثل `{count}` و`{name}`، وتسجيل مفاتيح غير المعروفة في وضع التطوير فقط. سيتم توفير دوال منفصلة لتنسيق العملة والأرقام والتواريخ حسب اللغة، بحيث يستخدم العربي locale عربيًا مع أرقام لاتينية حسب سياسة المشروع، ويستخدم الإنجليزي `en` مع رمز `SAR` بدل خلط الصيغ داخل النصوص.

ستُنقل الرسائل المتكررة، عناوين الأزرار، حالات التحميل، ورسائل الخطأ إلى القاموس. أما أسماء المطاعم والأصناف والأوصاف القادمة من قاعدة البيانات فستبقى بيانات محتوى وتستخدم مسار ترجمة المحتوى الموجود، لا قاموس واجهة ثابتًا.

### 4. دمج مبدل اللغات في الرأس والتنقل
سيُحافظ على `LanguageSwitcher` الحالي مع تحسينه ليعرض العربية والإنجليزية بوضوح، ويستخدم خصائص منطقية في موضع القائمة المنسدلة، ويغلق القائمة بطريقة لوحة مفاتيح سليمة. سيظهر المبدل في رأس لوحة الإدارة والمنيو العام وعلى الشاشات الصغيرة والكبيرة، من دون أن يحجب أزرار الحساب أو السلة أو نداء النادل.

سيتم التحقق من أن التبديل الفوري يحدّث العناوين والأزرار والـ placeholders و`aria-label` و`title`، مع إزالة الاعتماد على ترجمة DOM العامة للنصوص التي يمكن تحويلها إلى `t()` مباشرة. ستبقى آلية الترجمة القديمة كحل توافق مؤقت، لكن ستُقيد حتى لا تتسبب في بطء أو استبدال نصوص مرتين.

### 5. تطبيق RTL/LTR باستخدام الخصائص المنطقية
ستُراجع CSS العام ومكونات الواجهة لاستبدال `left/right` و`margin-left/right` و`padding-left/right` حيثما كانت تخص التخطيط، باستخدام `inset-inline-start/end` و`margin-inline-*` و`padding-inline-*` و`border-start-*`. لن تُحوّل المواضع التي تعتمد على معنى بصري ثابت، مثل صورة أو زر زخرفي، إلا بعد التحقق البصري.

سيُعتمد اتجاه حاوية الصفحة من السياق، وتُضاف قواعد اختبارية للنوافذ الجانبية والقوائم المنبثقة. يجب أن يفتح الـ sidebar من اليمين في العربية ومن اليسار في الإنجليزية، وأن ينعكس زر الإغلاق وفق الاتجاه: أعلى اليسار في RTL وأعلى اليمين في LTR، مع ضمان بقاء النوافذ داخل viewport وعدم ظهور تمرير أفقي.

### 6. معالجة المكونات الحساسة للاتجاه
ستُراجع مكونات `DashboardLayout` و`HomeSidebar` و`MobileNavigationDrawer` و`LanguageSwitcher` و`ManusDialog` و`Sheet/Dialog` وصفحة `RestaurantPublic`. سيتم تحديد موضع الإغلاق ديناميكيًا عبر `direction` أو خصائص منطقية، وتوحيد انتقالات off-canvas باستخدام حالات اتجاهية واضحة بدل قواعد متعارضة.

في المنيو العام ستُراجع بطاقة المنتج، نافذة تفاصيل المنتج، نافذة السلة، نموذج الحجز، رأس المنيو، وأزرار الحساب. ستبقى قاعدة نافذة تفاصيل المنتج الحالية: العربية من اليمين والإنجليزية من اليسار، مع اختبار أن تبديل اللغة أثناء وجود الصفحة لا يترك النافذة في الجهة القديمة.

### 7. تنسيق الأسعار والتواريخ
سيتم توحيد دالة تنسيق العملة لتنتج عرضًا واضحًا حسب اللغة، مثل `100.00 ر.س` للعربية و`100.00 SAR` للإنجليزية، مع تمرير رمز العملة من بيانات المطعم عند الحاجة. لن تُبنى نصوص الأسعار يدويًا داخل مكونات الصفحات. وستُراجع التواريخ وأرقام الطلبات والعدادات لضمان ثبات الأرقام وسلامة اتجاه النص المختلط باستخدام `dir="ltr"` عند الضرورة.

### 8. التحقق من النصوص الديناميكية وقاعدة البيانات
سيتم تحديد الحدود بين نصوص الواجهة الثابتة ومحتوى المطعم الديناميكي. ستستمر ترجمات الأصناف والتصنيفات والأوصاف عبر نموذج اللغات وقاعدة البيانات، مع مؤشر واضح للنصوص الناقصة داخل شاشة الترجمة. لن يُسمح بإظهار قيمة عربية خام في واجهة إنجليزية أو فرنسية عند وجود حقل ترجمة فارغ؛ سيظهر fallback محايد مع تسجيل العنصر للمراجعة.

### 9. الاختبارات
سيتم إضافة اختبارات وحدات لـ `LanguageContext` و`LanguageSwitcher` تغطي اختيار اللغة، التخزين، `document.dir/lang`، fallback، وقيمة المفتاح المفقود. وستُضاف اختبارات لعرض الأسعار بالعربية والإنجليزية، وللنوافذ الجانبية في الاتجاهين، وللقائمة المنسدلة وإمكانية الوصول.

سيتم تحديث اختبارات صفحات المنيو ولوحة الإدارة لتثبت عدم وجود نصوص مختلطة في مفاتيح الواجهة، وأن زر الإغلاق يتغير موضعه منطقيًا، وأن تبديل اللغة لا يسبب إعادة تحميل أو فقدان حالة السلة والنافذة. بعد ذلك سيُشغّل `pnpm exec tsc --noEmit` و`pnpm test` و`pnpm build`، ثم تُراجع المعاينة على مقاسات هاتف وسطح مكتب بالعربية والإنجليزية، مع اختبار الفرنسية إن بقيت ضمن اللغات المنشورة.

## قرارات مهمة

| المجال | القرار |
|---|---|
| مكتبة i18n | تطوير `LanguageContext` الحالي بدل إضافة مكتبة ثانية |
| مصدر نصوص الواجهة | ملفات لغة منظمة مع نوع مشترك وقاموس قاعدة البيانات للنصوص المُدارة |
| اللغة الافتراضية للوحة الإدارة | الإنجليزية كما هي سياسة المشروع الحالية، مع احترام التخزين السابق |
| لغة المنيو العام | اللغة اليدوية المحفوظة، ثم لغة المتصفح عند أول زيارة |
| اتجاه العربية | RTL |
| اتجاه الإنجليزية | LTR |
| التبديل | فوري داخل React، بلا إعادة تحميل كاملة |
| fallback | قيمة مترجمة من قاعدة البيانات، ثم ملف اللغة، ثم fallback آمن للمفتاح؛ لا عرض عربي صامت في LTR |
| الحركة | انتقالات قصيرة مع دعم `prefers-reduced-motion` |
| التوافق | الإبقاء على الفرنسية والـ bridge الحالي خلال الترحيل التدريجي |

## المخاطر والافتراضات

الافتراض الأساسي هو أن النصوص الديناميكية للمنتجات والتصنيفات تظل في نموذج المحتوى متعدد اللغات، ولا تُنقل إلى ملفات JSON. كما أن بعض المكونات القديمة تعتمد على ترجمة DOM العامة؛ إزالة هذا المسار دفعة واحدة قد تكشف نصوصًا غير مترجمة، لذلك سيتم ترحيلها تدريجيًا مع اختبار كل صفحة.

أكبر خطر بصري هو الخلط بين الخصائص المنطقية والمواضع الثابتة في مكونات RTL المعقدة، خصوصًا النوافذ الجانبية والقوائم المنبثقة. سيُعالج ذلك بقواعد اتجاهية صريحة واختبارات على الاتجاهين بدل تغيير شامل غير قابل للعزل. كما قد تظهر تحذيرات حجم الحزم بعد فصل ملفات اللغة؛ سيتم إبقاء القواميس مقسمة أو تحميلها عند الحاجة إذا أثّر ذلك في زمن فتح المنيو.

## مخرجات التنفيذ المتوقعة

سيكون الناتج طبقة لغة مركزية قابلة للتوسع، ملفات `ar.json` و`en.json` منظمة، مساعد `t()` مع fallback ومتغيرات، مبدل لغة متجاوب، اتجاه RTL/LTR موحد، نوافذ جانبية معكوسة صحيحة، تنسيق أسعار حسب locale، اختبارات regression، ومعاينة موثقة بالعربية والإنجليزية على الهاتف وسطح المكتب. لن يبدأ تنفيذ أي تعديل أو تغيير خارجي قبل اعتماد هذه الخطة.

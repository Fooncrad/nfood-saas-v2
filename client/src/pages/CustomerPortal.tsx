import { useMemo, useState } from "react";
import { Link } from "wouter";
import { Award, CalendarDays, Heart, MapPin, ReceiptText, Search, Share2, ShoppingBag, Sparkles, Store } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { toast } from "sonner";

export default function CustomerPortal() {
  const [query, setQuery] = useState("");
  const restaurants = trpc.platform.publicRestaurantDirectory.useQuery();
  const engagement = trpc.platform.engagement.useQuery();
  const referrals = trpc.platform.myReferrals.useQuery();
  const connectedRestaurants = useMemo(() => {
    const byRestaurant = new Map<number, { restaurantId: number; name: string; slug: string; logo?: string | null; color?: string | null; favorites: number; points?: number; tier?: string }>();
    for (const item of engagement.data?.favorites ?? []) {
      const current = byRestaurant.get(item.restaurantId) ?? { restaurantId: item.restaurantId, name: item.restaurantName, slug: item.restaurantSlug, logo: null, color: null, favorites: 0 };
      current.favorites += 1;
      byRestaurant.set(item.restaurantId, current);
    }
    for (const account of engagement.data?.loyalty ?? []) {
      const current = byRestaurant.get(account.restaurantId) ?? { restaurantId: account.restaurantId, name: account.restaurantName, slug: account.restaurantSlug, logo: account.brandLogoUrl, color: account.brandColor, favorites: 0 };
      current.points = account.pointsBalance;
      current.tier = account.tier;
      current.logo = account.brandLogoUrl;
      current.color = account.brandColor;
      byRestaurant.set(account.restaurantId, current);
    }
    return Array.from(byRestaurant.values()).sort((a, b) => (b.points ?? 0) - (a.points ?? 0) || b.favorites - a.favorites);
  }, [engagement.data]);
  const createReferralLink = trpc.platform.createMyReferralLink.useMutation();
  const shareRestaurant = async (restaurantId: number, restaurantName: string) => {
    try {
      const result = await createReferralLink.mutateAsync({ restaurantId });
      const shareData = { title: `رشّح ${restaurantName} عبر NFOOD`, text: `جرّب ${restaurantName} عبر NFOOD`, url: `${window.location.origin}${result.link}` };
      if (navigator.share) await navigator.share(shareData);
      else { await navigator.clipboard.writeText(shareData.url); toast.success("تم نسخ رابط الترشيح"); }
    } catch (error) {
      if (error instanceof DOMException && error.name === "AbortError") return;
      toast.error("تعذر إنشاء رابط الترشيح");
    }
  };
  const rows = useMemo(() => (restaurants.data ?? []).filter((restaurant) => `${restaurant.brandName ?? restaurant.name} ${restaurant.city ?? ""} ${restaurant.address ?? ""}`.toLowerCase().includes(query.toLowerCase())), [restaurants.data, query]);
  return <main dir="rtl" className="min-h-screen bg-[#f7f8fb] p-5 text-slate-900 sm:p-8"><div className="mx-auto max-w-6xl"><header className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold text-[#e76f3c]">NFOOD · بوابة العميل</p><h1 className="mt-2 text-3xl font-black">اكتشف مطاعمك واطلب بسهولة</h1><p className="mt-2 text-sm text-slate-500">تصفح المنيو، أضف للسلة، أرسل الطلب أو احجز من صفحة المطعم العامة.</p></div><div className="flex flex-wrap gap-2"><Link href="/customer-orders"><Button variant="outline" className="rounded-xl"><ShoppingBag className="ml-2 h-4 w-4" />سجل الطلبات</Button></Link><Link href="/customer-reservations"><Button variant="outline" className="rounded-xl"><CalendarDays className="ml-2 h-4 w-4" />حجوزاتي</Button></Link><Link href="/customer-rewards"><Button variant="outline" className="rounded-xl"><Award className="ml-2 h-4 w-4" />مكافآتي</Button></Link><Link href="/customer-content-orders"><Button variant="outline" className="rounded-xl"><ReceiptText className="ml-2 h-4 w-4" />طلبات المحتوى</Button></Link><Link href="/customer-profile"><Button variant="outline" className="rounded-xl">ملفي العام</Button></Link><Link href="/"><Button className="rounded-xl bg-[#e76f3c]">لوحتي</Button></Link></div></header>{(referrals.data?.length ?? 0) > 0 && <section className="mb-7 rounded-3xl border border-violet-100 bg-violet-50/70 p-5 shadow-sm"><div className="mb-3"><p className="text-xs font-black text-violet-700">ترشيحاتك للمطاعم</p><h2 className="mt-1 text-xl font-black text-slate-900">متابعة المكافآت</h2></div><div className="space-y-2">{(referrals.data ?? []).slice(0, 6).map((referral) => <div key={referral.id} className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-violet-100 bg-white p-3"><div><p className="font-black">{referral.restaurantName}</p><p className="mt-1 text-[11px] text-slate-500">{new Date(referral.createdAt).toLocaleDateString("ar-SA")} · الرمز {referral.code}</p></div><div className="text-left"><span className={`rounded-full px-2 py-1 text-[11px] font-bold ${referral.status === "rewarded" ? "bg-emerald-100 text-emerald-800" : referral.status === "qualified" ? "bg-amber-100 text-amber-800" : "bg-slate-100 text-slate-700"}`}>{referral.status === "rewarded" ? "تم صرف المكافأة" : referral.status === "qualified" ? "مؤهل للمكافأة" : referral.status === "cancelled" ? "ملغى" : "بانتظار أول طلب مؤهل"}</span>{referral.qualifiedAt && <p className="mt-1 text-[10px] text-slate-500">تأهل في {new Date(referral.qualifiedAt).toLocaleDateString("ar-SA")}</p>}</div></div>)}</div></section>}{connectedRestaurants.length > 0 && <section className="mb-7 rounded-3xl border border-orange-100 bg-gradient-to-l from-orange-50 via-white to-emerald-50 p-5 shadow-sm"><div className="mb-4 flex items-center justify-between gap-3"><div><p className="flex items-center gap-2 text-xs font-black text-orange-700"><Sparkles className="h-4 w-4" /> مساحتك في NFOOD</p><h2 className="mt-1 text-xl font-black">مطاعمك وتقدم الولاء</h2></div><Heart className="h-5 w-5 text-rose-500" /></div><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{connectedRestaurants.map((restaurant) => <Card key={restaurant.restaurantId} className="overflow-hidden rounded-2xl border-white/80 bg-white/80 shadow-sm"><CardContent className="p-4"><div className="flex items-center gap-3"><div className="flex h-11 w-11 shrink-0 items-center justify-center overflow-hidden rounded-xl text-white" style={{ backgroundColor: restaurant.color ?? "#e76f3c" }}>{restaurant.logo ? <img src={restaurant.logo} alt="" onError={(event) => { event.currentTarget.style.display = "none"; }} className="h-full w-full object-contain p-1" /> : <Store className="h-5 w-5" />}</div><div className="min-w-0"><p className="truncate font-black">{restaurant.name}</p><p className="mt-1 text-[11px] text-slate-500">{restaurant.favorites ? `${restaurant.favorites} أصناف محفوظة` : "تفاعل محفوظ"}</p></div></div>{restaurant.points !== undefined && <div className="mt-4 flex items-center justify-between rounded-xl bg-amber-50 px-3 py-2"><span className="flex items-center gap-1 text-xs font-bold text-amber-800"><Award className="h-4 w-4" />{restaurant.tier === "gold" ? "ذهبي" : restaurant.tier === "silver" ? "فضي" : "قياسي"}</span><strong className="text-sm text-amber-900">{restaurant.points} نقطة</strong></div>}<Link href={`/restaurant/${restaurant.slug}`}><Button variant="outline" className="mt-3 w-full rounded-xl text-xs">فتح منيو المطعم</Button></Link></CardContent></Card>)}</div></section>}<div className="mb-6 flex items-center gap-3 rounded-2xl border border-slate-200 bg-white px-4 py-3 shadow-sm"><Search className="h-5 w-5 text-slate-400" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="ابحث باسم المطعم أو المدينة" className="h-8 border-0 p-0 shadow-none focus-visible:ring-0" /></div>{restaurants.isLoading ? <div className="grid gap-4 md:grid-cols-3">{[1,2,3].map((item) => <div key={item} className="h-56 animate-pulse rounded-3xl bg-white" />)}</div> : restaurants.isError ? <Card className="rounded-3xl"><CardContent className="p-8 text-center text-sm text-red-600">تعذر تحميل دليل المطاعم. Request ID: customer-directory</CardContent></Card> : rows.length === 0 ? <Card className="rounded-3xl"><CardContent className="p-12 text-center"><Store className="mx-auto h-10 w-10 text-slate-300" /><p className="mt-3 text-sm text-slate-500">لا توجد مطاعم مطابقة حاليًا.</p></CardContent></Card> : <div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{rows.map((restaurant) => <Card key={restaurant.id} className="overflow-hidden rounded-3xl border-slate-200 bg-white shadow-sm"><div className="h-28" style={{ background: `linear-gradient(135deg, ${restaurant.brandColor ?? "#e76f3c"}, #101d31)` }}>{restaurant.brandLogoUrl && <img src={restaurant.brandLogoUrl} alt="" onError={(event) => { event.currentTarget.style.display = "none"; }} className="h-full w-full object-contain p-6" />}</div><CardContent className="p-5"><h2 className="text-lg font-black">{restaurant.brandName ?? restaurant.name}</h2><p className="mt-2 line-clamp-2 text-xs leading-6 text-slate-500">{restaurant.brandDescription || "صفحة مطعم عامة تشمل المنيو والطلب والحجز."}</p>{(restaurant.city || restaurant.address) && <p className="mt-3 flex items-center gap-1 text-xs text-slate-500"><MapPin className="h-4 w-4 text-[#e76f3c]" />{[restaurant.city, restaurant.address].filter(Boolean).join(" · ")}</p>}<div className="mt-5 grid grid-cols-2 gap-2"><Link href={`/restaurant/${restaurant.slug}`}><Button className="w-full rounded-xl bg-[#e76f3c]"><ShoppingBag className="ml-1 h-4 w-4" />المنيو والطلب</Button></Link><Link href={`/restaurant/${restaurant.slug}#reservation`}><Button variant="outline" className="w-full rounded-xl"><CalendarDays className="ml-1 h-4 w-4" />الحجز</Button></Link></div><Button type="button" variant="ghost" disabled={createReferralLink.isPending} onClick={() => void shareRestaurant(restaurant.id, restaurant.brandName ?? restaurant.name)} className="mt-2 w-full rounded-xl text-xs text-orange-700 hover:bg-orange-50"><Share2 className="ml-1 h-4 w-4" />{createReferralLink.isPending ? "جارٍ تجهيز الرابط..." : "رشّح هذا المطعم"}</Button></CardContent></Card>)}</div>}</div></main>;
}

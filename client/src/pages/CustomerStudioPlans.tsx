import { Check, Crown, HardDrive, Headphones, Sparkles } from "lucide-react";
import { Link } from "wouter";
import { useState } from "react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";

const plans = [
  { key: "studio-100", name: "البداية", size: 100, price: "مجانية", description: "لبدء تصوير ونشر محتوى الطعام", features: ["رفع صور من الكاميرا", "فحص وعلامة مائية", "مكتبة أساسية"] },
  { key: "studio-500", name: "صانع المحتوى", size: 500, price: "19.00 ر.س / شهر", description: "لصناع المحتوى النشطين", features: ["مساحة أكبر للمكتبة", "أولوية في المعالجة", "تتبع المبيعات والمكافآت"] },
  { key: "studio-2000", name: "Trend Pro", size: 2000, price: "49.00 ر.س / شهر", description: "لمنشئي المحتوى ذوي النشاط العالي", features: ["مساحة واسعة", "دعم أسرع", "تقارير مبيعات موسعة"] },
];

export default function CustomerStudioPlans() {
  const settings = trpc.platform.myPreferences.useQuery(undefined, { retry: false });
  const [selected, setSelected] = useState("studio-500");
  const currentLimit = 100;
  const requestUpgrade = () => { const plan = plans.find((item) => item.key === selected); if (!plan) return; toast.success(`تم تجهيز طلب ترقية ${plan.name}. سيتم تحويلك إلى مركز الدعم لإكمال الدفع والمراجعة.`); window.setTimeout(() => { window.location.href = `/support?topic=studio-upgrade&plan=${plan.key}`; }, 650); };
  return <main dir="rtl" className="min-h-screen bg-[#f7f8fb] p-5 text-slate-900 sm:p-8"><div className="mx-auto max-w-6xl"><header className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[0.16em] text-[#e76f3c]">NFOOD · Studio Space</p><h1 className="mt-2 text-3xl font-black">باقات مساحة Studio</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">اختر المساحة المناسبة لصورك الغذائية. تتم مراجعة طلب الترقية والدفع بأمان عبر مركز الدعم والتشخيص.</p></div><Link href="/customer-studio"><Button variant="outline" className="rounded-xl">العودة إلى الاستديو</Button></Link></header><Card className="mb-6 overflow-hidden rounded-3xl border-orange-100 bg-gradient-to-l from-orange-50 via-white to-emerald-50 shadow-sm"><CardContent className="flex flex-wrap items-center justify-between gap-5 p-5"><div className="flex items-center gap-3"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-orange-100 text-[#e76f3c]"><HardDrive className="h-6 w-6" /></div><div><p className="text-xs font-bold text-slate-500">باقتك الحالية</p><p className="mt-1 text-xl font-black">{currentLimit} MB <span className="text-sm font-bold text-slate-500">مساحة Studio</span></p></div></div><div className="rounded-2xl bg-white/80 px-4 py-3 text-xs text-slate-600 shadow-sm"><p className="font-black text-slate-900">لا تفقد صورك</p><p className="mt-1">الترقية توسع مساحة الرفع والمكتبة دون حذف المحتوى.</p></div></CardContent></Card><div className="grid gap-5 lg:grid-cols-3">{plans.map((plan) => <Card key={plan.key} className={`relative rounded-3xl bg-white shadow-sm transition ${selected === plan.key ? "border-2 border-[#e76f3c] shadow-orange-100" : "border-slate-200"}`}><CardHeader><div className="flex items-start justify-between gap-3"><div><CardTitle className="text-xl font-black">{plan.name}</CardTitle><p className="mt-1 text-xs text-slate-500">{plan.description}</p></div>{plan.key === "studio-500" ? <Crown className="h-6 w-6 text-[#e76f3c]" /> : <Sparkles className="h-5 w-5 text-slate-300" />}</div><p className="mt-5 text-3xl font-black text-slate-950">{plan.size}<span className="mr-1 text-sm font-bold text-slate-500">MB</span></p><p className="mt-1 text-sm font-bold text-[#c75325]">{plan.price}</p></CardHeader><CardContent><div className="space-y-2 text-xs text-slate-600">{plan.features.map((feature) => <p key={feature} className="flex items-center gap-2"><Check className="h-4 w-4 text-emerald-600" />{feature}</p>)}</div><Button type="button" onClick={() => setSelected(plan.key)} variant={selected === plan.key ? "default" : "outline"} className={`mt-6 w-full rounded-xl ${selected === plan.key ? "bg-[#e76f3c] hover:bg-[#d85f2e]" : ""}`}>{selected === plan.key ? "الباقة المحددة" : "اختيار الباقة"}</Button></CardContent></Card>)}</div><section className="mt-6 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="flex items-center gap-2 text-sm font-black"><Headphones className="h-5 w-5 text-[#e76f3c]" />إكمال الترقية</p><p className="mt-1 text-xs leading-6 text-slate-500">بعد اختيار الباقة، يراجع فريق الإدارة الطلب ويؤكد السعر وطريقة الدفع قبل تفعيل المساحة. لا يتم الخصم تلقائيًا من هذه الصفحة.</p></div><Button type="button" onClick={requestUpgrade} className="rounded-xl bg-[#101d31] px-6 text-xs font-black hover:bg-slate-800">طلب الترقية والتواصل مع الدعم</Button></div></section><p className="mt-5 text-center text-[11px] text-slate-400">العملة: SAR · {settings.isLoading ? "جارٍ التحقق من إعدادات الحساب" : "مساحتك مرتبطة بحساب العميل الحالي"}</p></div></main>;
}

import { useMemo, useState } from "react";
import { Building2, Check, ChevronDown, Copy, Edit3, ExternalLink, KeyRound, LogIn, MoreHorizontal, Plus, Power, Search, Store, Trash2, Utensils, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { toast } from "sonner";
import { CreateRestaurantDialog } from "@/components/CreateRestaurantDialog";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";

type Filter = "الكل" | "نشط" | "تجربة" | "معلّق";

type PlanRecord = {
  id: number;
  key: string;
  name: string;
  description: string | null;
  monthlyPrice: string;
  yearlyPrice: string;
  isActive: boolean;
  features: Array<{ key: string; enabled: boolean; featureLimit: number | null }>;
};

type FeatureDefinition = { id: number; key: string; label: string; dependencyKey: string | null; defaultLimit: number | null; isAddOn: boolean; addonPrice: string | null };

export function SuperAdminRestaurantCatalog() {
  const [query, setQuery] = useState("");
  const [filter, setFilter] = useState<Filter>("الكل");
  const [createOpen, setCreateOpen] = useState(false);
  const [expandedPlan, setExpandedPlan] = useState<number | null>(null);
  const [credentials, setCredentials] = useState<{ email: string; password: string } | null>(null);
  const restaurantsQuery = trpc.admin.restaurants.useQuery(undefined, { retry: 2 });
  const plansQuery = trpc.admin.packagePlans.useQuery(undefined, { retry: 2 });
  const definitionsQuery = trpc.admin.featureDefinitions.useQuery(undefined, { retry: 2 });
  const utils = trpc.useUtils();
  const updateRestaurant = trpc.admin.updateRestaurant.useMutation({ onSuccess: () => { void utils.admin.restaurants.invalidate(); toast.success("تم تحديث المطعم"); }, onError: (error) => toast.error(`تعذر تحديث المطعم: ${error.message}`) });
  const enterRestaurant = trpc.admin.enterRestaurantAccount.useMutation({ onSuccess: () => { toast.success("تم الدخول إلى مساحة المطعم"); window.location.assign("/"); }, onError: (error) => toast.error(`تعذر الدخول إلى المطعم: ${error.message}`) });
  const deleteRestaurant = trpc.admin.deleteRestaurant.useMutation({ onSuccess: () => { void utils.admin.restaurants.invalidate(); toast.success("تم حذف المطعم بأمان"); }, onError: (error) => toast.error(`تعذر حذف المطعم: ${error.message}`) });
  const createRestaurant = trpc.admin.createRestaurant.useMutation({ onSuccess: (data) => { void utils.admin.restaurants.invalidate(); setCreateOpen(false); setCredentials({ email: data.account.email, password: data.account.temporaryPassword }); toast.success("تم إنشاء المطعم وحساب الدخول"); }, onError: (error) => toast.error(`تعذر إنشاء المطعم: ${error.message}`) });
  const resetPassword = trpc.admin.resetRestaurantPassword.useMutation({ onSuccess: (data) => { setCredentials({ email: data.email, password: data.temporaryPassword }); toast.success("تم تحديث كلمة المرور وإبطال الجلسات السابقة"); }, onError: (error) => toast.error(`تعذر إعادة تعيين كلمة المرور: ${error.message}`) });
  const rows = useMemo(() => (restaurantsQuery.data ?? []).filter((restaurant) => { const status = restaurant.status === "active" ? "نشط" : restaurant.status === "trial" ? "تجربة" : "معلّق"; const matchesFilter = filter === "الكل" || filter === status; const text = `${restaurant.name} ${restaurant.slug ?? ""} ${restaurant.plan ?? ""}`.toLowerCase(); return matchesFilter && text.includes(query.trim().toLowerCase()); }), [filter, query, restaurantsQuery.data]);
  const plans = (plansQuery.data ?? []) as PlanRecord[];
  const definitions = (definitionsQuery.data ?? []) as FeatureDefinition[];
  const linkedFeatures = useMemo(() => plans.reduce((total, plan) => total + plan.features.filter((feature) => feature.enabled).length, 0), [plans]);
  const catalogLoading = plansQuery.isLoading || definitionsQuery.isLoading;
  const catalogError = plansQuery.isError || definitionsQuery.isError;

  return <div dir="rtl" className="space-y-5">
    <div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-bold text-[#e76f3c]">مركز المطاعم</p><h2 className="mt-1 text-2xl font-black text-slate-900">قائمة المطاعم</h2><p className="mt-1 text-sm text-slate-500">إدارة المطاعم المسجلة، الباقات، الحالة، والروابط العامة من شاشة واحدة.</p></div><Button onClick={() => setCreateOpen(true)} className="gap-2 rounded-xl bg-[#e76f3c] shadow-sm hover:bg-[#d85f2e]"><Plus className="h-4 w-4" /> إضافة مطعم جديد</Button></div>
    <CreateRestaurantDialog open={createOpen} pending={createRestaurant.isPending} onClose={() => setCreateOpen(false)} onSubmit={(input) => createRestaurant.mutate(input)} />
    <Dialog open={Boolean(credentials)} onOpenChange={(open) => { if (!open) setCredentials(null); }}><DialogContent dir="rtl" className="rounded-3xl border-slate-200 bg-white sm:max-w-md"><DialogHeader className="text-right"><DialogTitle className="text-xl font-black text-slate-900">بيانات دخول المطعم</DialogTitle><DialogDescription className="leading-6">احفظ هذه البيانات الآن؛ لن نعرض كلمة المرور مرة أخرى بعد إغلاق هذه النافذة.</DialogDescription></DialogHeader>{credentials && <div className="space-y-3 rounded-2xl bg-slate-50 p-4"><div><p className="mb-1 text-xs font-bold text-slate-500">البريد الإلكتروني</p><div className="flex items-center gap-2"><code dir="ltr" className="flex-1 rounded-xl bg-white px-3 py-2 text-sm text-slate-800">{credentials.email}</code><Button type="button" size="icon" variant="outline" aria-label="نسخ البريد" onClick={() => void navigator.clipboard?.writeText(credentials.email)}><Copy className="h-4 w-4" /></Button></div></div><div><p className="mb-1 text-xs font-bold text-slate-500">كلمة المرور</p><div className="flex items-center gap-2"><code dir="ltr" className="flex-1 rounded-xl bg-white px-3 py-2 text-sm text-slate-800">{credentials.password}</code><Button type="button" size="icon" variant="outline" aria-label="نسخ كلمة المرور" onClick={() => void navigator.clipboard?.writeText(credentials.password)}><Copy className="h-4 w-4" /></Button></div></div></div>}<DialogFooter><Button type="button" onClick={() => setCredentials(null)} className="rounded-xl bg-[#e76f3c] hover:bg-[#d85f2e]">تم الحفظ</Button></DialogFooter></DialogContent></Dialog>
    <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[{ label: "إجمالي المطاعم", value: restaurantsQuery.data?.length ?? 0, icon: Store }, { label: "نشطة", value: restaurantsQuery.data?.filter((item) => item.status === "active").length ?? 0, icon: Check }, { label: "تجريبية", value: restaurantsQuery.data?.filter((item) => item.status === "trial").length ?? 0, icon: Building2 }, { label: "مميزات مفعلة في الباقات", value: linkedFeatures, icon: Utensils }].map(({ label, value, icon: Icon }) => <Card key={label} className="rounded-2xl border-slate-200 bg-white shadow-sm"><CardContent className="flex items-center justify-between p-4"><div><p className="text-xs text-slate-500">{label}</p><p className="mt-2 text-2xl font-black text-slate-900">{value}</p></div><div className="rounded-xl bg-orange-50 p-3 text-[#e76f3c]"><Icon className="h-5 w-5" /></div></CardContent></Card>)}</div>
    <Card className="overflow-hidden rounded-2xl border-slate-200 bg-white shadow-sm"><CardHeader className="border-b border-slate-100 p-4"><div className="flex flex-wrap items-center gap-3"><div className="relative min-w-[240px] flex-1"><Search className="pointer-events-none absolute right-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="ابحث باسم المطعم أو المعرّف أو الباقة" className="h-11 rounded-xl pr-9" /></div><div className="flex flex-wrap gap-2">{(["الكل", "نشط", "تجربة", "معلّق"] as Filter[]).map((item) => <button key={item} type="button" onClick={() => setFilter(item)} className={`rounded-xl px-3 py-2 text-xs font-bold transition ${filter === item ? "bg-[#e76f3c] text-white" : "bg-slate-100 text-slate-500 hover:bg-orange-50 hover:text-[#e76f3c]"}`}>{item}</button>)}</div><Button variant="outline" className="gap-2 rounded-xl"><MoreHorizontal className="h-4 w-4" /> إجراءات</Button></div></CardHeader><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[980px] text-right text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr><th className="px-5 py-3 font-bold">#</th><th className="px-5 py-3 font-bold">المطعم</th><th className="px-5 py-3 font-bold">المعرّف / الرابط</th><th className="px-5 py-3 font-bold">الباقة</th><th className="px-5 py-3 font-bold">الفروع</th><th className="px-5 py-3 font-bold">الحالة</th><th className="px-5 py-3 font-bold">الإجراءات</th></tr></thead><tbody className="divide-y divide-slate-100">{restaurantsQuery.isLoading ? <tr><td colSpan={7} className="px-5 py-12 text-center text-sm text-slate-400">جارٍ تحميل المطاعم...</td></tr> : restaurantsQuery.isError ? <tr><td colSpan={7} className="px-5 py-12 text-center text-sm text-red-600">تعذر تحميل القائمة. Request ID: admin-restaurants <button type="button" onClick={() => void restaurantsQuery.refetch()} className="mr-2 font-bold underline">إعادة المحاولة</button></td></tr> : rows.length === 0 ? <tr><td colSpan={7} className="px-5 py-12 text-center text-sm text-slate-500">لا توجد مطاعم مطابقة للبحث الحالي.</td></tr> : rows.map((restaurant, index) => { const status = restaurant.status === "active" ? "نشط" : restaurant.status === "trial" ? "تجربة" : "معلّق"; return <tr key={restaurant.id} className="transition hover:bg-orange-50/40"><td className="px-5 py-4 text-xs text-slate-400">{index + 1}</td><td className="px-5 py-4"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-orange-50 text-[#e76f3c]"><Store className="h-4 w-4" /></div><div><p className="font-bold text-slate-800">{restaurant.name}</p><p className="mt-0.5 text-[11px] text-slate-400">حساب #{restaurant.id}</p></div></div></td><td className="px-5 py-4"><div className="flex flex-col items-start gap-2"><a href={`/menu/${encodeURIComponent(restaurant.slug)}`} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 font-mono text-xs text-sky-700 hover:underline" dir="ltr">/{restaurant.slug}<ExternalLink className="h-3 w-3" /></a><a href={`/menu/${encodeURIComponent(restaurant.slug)}`} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 text-[11px] font-bold text-[#e76f3c] hover:underline"><Utensils className="h-3.5 w-3.5" /> فتح Menu</a></div></td><td className="px-5 py-4"><Badge variant="outline" className="rounded-lg border-sky-200 bg-sky-50 text-sky-700">{restaurant.plan ?? "غير محددة"}</Badge></td><td className="px-5 py-4 text-slate-600">{restaurant.branchCount ?? 0}</td><td className="px-5 py-4"><Badge variant="outline" className={`rounded-lg ${status === "نشط" ? "border-emerald-200 bg-emerald-50 text-emerald-700" : status === "تجربة" ? "border-amber-200 bg-amber-50 text-amber-700" : "border-slate-200 bg-slate-50 text-slate-600"}`}>{status}</Badge></td><td className="px-5 py-4"><div className="flex flex-wrap items-center gap-2"><Button type="button" size="sm" disabled={enterRestaurant.isPending} onClick={() => enterRestaurant.mutate({ id: restaurant.id })} className="gap-1 rounded-lg bg-[#111c2e] text-xs text-white hover:bg-[#1b2a43]"><LogIn className="h-3.5 w-3.5" /> دخول المطعم</Button><Button type="button" size="sm" variant="outline" disabled={updateRestaurant.isPending} onClick={() => updateRestaurant.mutate({ id: restaurant.id, status: restaurant.status === "active" ? "trial" : "active" })} className="gap-1 rounded-lg text-xs"><Power className="h-3.5 w-3.5" /> {restaurant.status === "active" ? "إيقاف مؤقت" : "تفعيل المطعم"}</Button><Button type="button" size="sm" variant="outline" onClick={() => updateRestaurant.mutate({ id: restaurant.id, name: restaurant.name, plan: restaurant.plan ?? "Growth" })} className="gap-1 rounded-lg text-xs"><Edit3 className="h-3.5 w-3.5" /> حفظ الباقة</Button><Button type="button" size="sm" variant="outline" disabled={resetPassword.isPending} onClick={() => { const password = window.prompt("أدخل كلمة المرور الجديدة (6 أحرف أو أرقام على الأقل):"); if (password && password.length >= 6) resetPassword.mutate({ restaurantId: restaurant.id, password }); else if (password) toast.error("كلمة المرور يجب أن تتكون من 6 أحرف أو أرقام على الأقل"); }} className="gap-1 rounded-lg text-xs"><KeyRound className="h-3.5 w-3.5" /> إعادة تعيين كلمة المرور</Button><Button type="button" size="sm" variant="outline" onClick={() => { if (window.confirm("سيتم حذف المطعم بأمان مع الحفاظ على السجل؟")) deleteRestaurant.mutate({ id: restaurant.id }); }} className="gap-1 rounded-lg text-xs text-red-600"><Trash2 className="h-3.5 w-3.5" /> حذف</Button></div></td></tr>; })}</tbody></table></div></CardContent></Card>
    <div className="grid gap-5 xl:grid-cols-[1.2fr_1fr]"><Card className="overflow-hidden rounded-2xl border-slate-200 bg-white shadow-sm"><CardHeader className="border-b border-slate-100 bg-gradient-to-l from-cyan-50 via-white to-white p-4"><div className="flex items-center justify-between gap-3"><div><CardTitle className="text-base">مميزات الباقات الفعلية</CardTitle><p className="mt-1 text-xs text-slate-500">هذه البيانات تُقرأ مباشرة من packagePlans وpackagePlanFeatures وfeatureDefinitions.</p></div><Badge variant="outline" className="rounded-lg">{catalogLoading ? "جارٍ التحميل" : `${plans.length} باقات · ${definitions.length} ميزة`}</Badge></div></CardHeader><CardContent className="p-0">{catalogError ? <div className="m-4 rounded-2xl border border-red-200 bg-red-50 p-5 text-sm text-red-700">تعذر تحميل كتالوج الباقات. Request ID: package-catalog <button type="button" onClick={() => { void plansQuery.refetch(); void definitionsQuery.refetch(); }} className="mr-2 font-bold underline">إعادة المحاولة</button></div> : catalogLoading ? <div className="space-y-2 p-4"><div className="h-14 animate-pulse rounded-xl bg-slate-100" /><div className="h-14 animate-pulse rounded-xl bg-slate-100" /></div> : plans.length === 0 ? <div className="p-7 text-center"><p className="text-sm text-slate-600">لا توجد باقات محفوظة حاليًا.</p><p className="mt-1 text-xs text-slate-400">استخدم محرر الباقات لإنشاء أول باقة وربط المميزات بها.</p></div> : plans.map((plan, index) => { const featureMap = new Map(plan.features.map((feature) => [feature.key, feature])); const enabledCount = plan.features.filter((feature) => feature.enabled).length; const isExpanded = expandedPlan === plan.id; return <div key={plan.id} className="border-b border-slate-100 last:border-b-0"><button type="button" onClick={() => setExpandedPlan(isExpanded ? null : plan.id)} className="flex w-full items-center justify-between gap-3 p-4 text-right hover:bg-slate-50"><span className="flex min-w-0 items-center gap-3"><span className={`h-2.5 w-2.5 shrink-0 rounded-full ${index % 3 === 0 ? "bg-slate-400" : index % 3 === 1 ? "bg-sky-500" : "bg-violet-500"}`} /><span className="min-w-0"><span className="block truncate font-bold text-slate-800">{plan.name}</span><span className="mt-0.5 block truncate font-mono text-[10px] text-slate-400">{plan.key} · {plan.monthlyPrice} ر.س/شهري · {enabledCount} مفعّلة</span></span></span><span className="flex items-center gap-2"><Badge className={`rounded-lg ${plan.isActive ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-500"}`}>{plan.isActive ? "نشطة" : "متوقفة"}</Badge><ChevronDown className={`h-4 w-4 transition ${isExpanded ? "rotate-180" : ""}`} /></span></button>{isExpanded && <div className="grid gap-2 bg-slate-50/60 p-4 sm:grid-cols-2">{definitions.map((definition) => { const link = featureMap.get(definition.key); const enabled = link?.enabled === true; return <div key={definition.id} className={`flex items-center justify-between gap-2 rounded-xl border px-3 py-2 text-xs ${enabled ? "border-emerald-200 bg-emerald-50/70" : "border-slate-200 bg-white"}`}><span className={`flex min-w-0 items-center gap-2 font-semibold ${enabled ? "text-emerald-800" : "text-slate-400"}`}>{enabled ? <Check className="h-3.5 w-3.5 shrink-0" /> : <X className="h-3.5 w-3.5 shrink-0" />}<span className="truncate">{definition.label}</span></span><span className="shrink-0 font-mono text-[10px] text-slate-400">{enabled && link?.featureLimit !== null && link?.featureLimit !== undefined ? `حد ${link.featureLimit}` : definition.isAddOn ? "إضافة" : "—"}</span></div>; })}</div>}</div>; })}</CardContent></Card>
      <Card className="overflow-hidden rounded-2xl border-slate-200 bg-white shadow-sm"><CardHeader className="border-b border-slate-100 bg-gradient-to-l from-emerald-50 via-white to-white p-4"><CardTitle className="text-base">كتالوج المميزات المتاحة</CardTitle><p className="mt-1 text-xs text-slate-500">المفاتيح الموجودة حاليًا في قاعدة البيانات، مع التبعيات والإضافات وأسعارها.</p></CardHeader><CardContent className="p-4">{catalogError ? <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">تعذر تحميل تعريفات المميزات. Request ID: feature-definitions</p> : catalogLoading ? <div className="h-28 animate-pulse rounded-xl bg-slate-100" /> : definitions.length === 0 ? <p className="rounded-xl bg-slate-50 p-6 text-center text-sm text-slate-500">لا توجد مميزات معرفة بعد.</p> : <div className="space-y-2">{definitions.map((definition) => <div key={definition.id} className="rounded-xl border border-slate-100 bg-slate-50/70 p-3"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="truncate text-sm font-bold text-slate-800">{definition.label}</p><p className="mt-1 truncate font-mono text-[10px] text-slate-400">{definition.key}</p></div><Badge variant="outline" className="shrink-0 rounded-lg">{definition.isAddOn ? `إضافة ${definition.addonPrice ?? "0"} ر.س` : "ضمن الباقة"}</Badge></div>{definition.dependencyKey ? <p className="mt-2 text-[11px] text-slate-500">يعتمد على: <span className="font-mono">{definition.dependencyKey}</span></p> : null}</div>)}</div>}</CardContent></Card></div>
  </div>;
}

# تقرير تنفيذي شامل عن نظام NFOOD

**إعداد:** Manus AI  
**تاريخ التقرير:** 8 سبتمبر 2026  
**المشروع:** `nfood-restaurant-saas`  
**النطاق:** منصة SaaS عربية RTL لإدارة المطاعم والفروع والمنيو والطلبات والموظفين والتسويق وشاشات العرض.

## 1. الملخص التنفيذي

NFOOD هو نظام متكامل لإدارة عمليات المطاعم، مبني على React 19 وTypeScript وtRPC 11 وDrizzle ORM وMySQL، مع خادم Express ومصادقة Manus وتخزين ملفات عبر طبقة التخزين المدمجة. المشروع في حالة تشغيلية متقدمة، ويحتوي على لوحات متعددة للأدمن والمطعم والموظفين والعملاء، إضافة إلى منظومة منيو عامة، طلبات، حجوزات، توصيل، مطبخ، نقاط بيع، ترجمة، تسويق وشاشات عرض.

الحالة الفنية الحالية **مستقرة من ناحية التحقق البرمجي**: فحص TypeScript ينجح دون أخطاء، ومجموعة الاختبارات الحالية تنجح بالكامل بعدد **692 اختبارًا في 166 ملفًا**. آخر نسخة محفوظة في Git هي `1fb59f7`، وتخص إصلاح التخطيط الثنائي لشاشة العرض بحيث يظهر الفيديو في اليسار والصورة المرتبطة به في اليمين مع fallback للصورة.[^1]

> **الخلاصة العملية:** النظام صالح لمواصلة الاختبار التشغيلي والعرض، لكنه لا ينبغي اعتباره مكتملًا إنتاجيًا قبل إغلاق اختبارات الأجهزة الحقيقية، ورفع الفيديو الدائم من لوحة التحكم، واختبارات POS والتوصيل والترجمة المتقدمة والصلاحيات الحية.

## 2. البنية التقنية

| المجال | الوضع الحالي |
|---|---|
| الواجهة | React 19 وVite وTailwind CSS 4 ومكونات Radix/shadcn-style |
| الخادم | Express 4 وtRPC 11 وTypeScript |
| قاعدة البيانات | MySQL/TiDB عبر Drizzle ORM والمخططات والترحيلات |
| المصادقة | Manus OAuth، جلسات محمية، حسابات اختبار وأدوار متعددة |
| التخزين | `storagePut` عبر تخزين Manus/S3، وروابط `/manus-storage/...` |
| الاختبارات | Vitest، مع اختبارات Playwright متاحة عبر script مستقل |
| التحديث الفوري | WebSocket/Realtime لمزامنة شاشات العرض والتنبيهات |
| التشغيل | Autoscale مع نشر تلقائي عند حفظ checkpoint |
| اللغات | بنية قاموس JSON ومكونات لإدارة الترجمة والمراجعة |

الملف `package.json` يوفّر أوامر التحقق الأساسية: `pnpm run check` لفحص TypeScript، و`pnpm test` لتشغيل Vitest، و`pnpm run e2e` لاختبارات Playwright، إضافة إلى أوامر فحص وإضافة مفاتيح الترجمة.[^2]

## 3. الوحدات الوظيفية

### 3.1 الإدارة والمنصة والمطاعم

توجد لوحات للأدمن والمنصة وإدارة المطاعم والفروع، مع كتالوج للمطاعم، إعدادات الباقات، الصلاحيات، الهوية البصرية، العلامة التجارية، إعدادات العرض، ومكونات لإدارة حسابات الفريق. كما توجد مكونات منفصلة لإدارة المطعم، العملاء، الصلاحيات، فريق العمل، إعدادات الأسعار، القوالب، ومساحات المنصة.

### 3.2 المنيو العام وتجربة العميل

المنيو العام يدعم RTL، الفئات، البحث، القوالب الشبكية، البطاقات الكثيفة على سطح المكتب، العرض الثنائي على الجوال، تفاصيل الصنف، السلة، أنواع الطلب، QR، الهوية البصرية، اللغات، العملات، وحالات التوفر. توجد اختبارات مخصصة لتخطيط بطاقات المنتجات، تجربة المنيو العام، روابط المنيو، RTL، التخصيص، والمكونات العامة.

### 3.3 الطلبات ونقاط البيع والمطبخ

يتضمن المشروع مسارات الطلبات، التسعير، المدفوعات، الفواتير، الطباعة، KDS، محطات المطبخ، الطلبات متعددة الأقسام، والتخزين المؤقت للعمل دون اتصال. توجد اختبارات منفصلة للتخزين المؤقت في POS، التسعير، المدفوعات، المطبخ، الطباعة، والتنبيهات الفورية. ما يزال الاختبار اليدوي على جهاز POS أثناء قطع الاتصال وإعادته بندًا مفتوحًا، لذلك يجب اعتبار الدعم البرمجي موجودًا لكن التحقق الميداني غير مكتمل.

### 3.4 الحجوزات

توجد واجهات وسياسات للحجوزات، تقويم شهري، أوقات الفتح والإغلاق، مبالغ إضافية، قبول ورفض وتعديل الحجوزات، وقواعد الإلغاء. كما توجد اختبارات لسياسات الحجوزات، التقويم، إشعارات الحجوزات، ومسارات الحجوزات العامة.

### 3.5 النادل والسائق والتوصيل

يضم المشروع أقسامًا لنداء النادل، إحصاءات زمن الاستجابة، حسابات الفريق، السائقين، التوصيل، التتبع، المحاسبة، الديون والعمولات، وتنبيهات الطلبات. توجد اختبارات لحركة الطلب والتتبع وتدفق السائق، لكن بعض الاختبارات التشغيلية الحية ما تزال مطلوبة، خصوصًا وصول الطلبات لجميع السائقين، حجب بيانات الخريطة غير المصرح بها، وتجربة نداء النادل مع الصوت والاهتزاز.

### 3.6 الترجمة واللغات

توجد بنية ترجمة مركزية، محرر ترجمة، مراجعة واعتماد، قاموس، إدارة لغات المنيو، تخزين مؤقت للترجمة، واختبارات تغطية ومصدر وسياسة الترجمة. المتبقي هو ربط ترجمة آلية متقدمة عبر مزود خارجي بمفتاح آمن، مع fallback للقاموس اليدوي ومنع ظهور النص الأصلي غير المصقول عند فشل المزود.

### 3.7 الملفات والصور وNFC وvCard

توجد مكتبة وسائط، رفع صور، قص وضغط، أرشفة، QR، vCard، NFC وبطاقات مادية، مع طبقات تحقق واختبارات للملفات والصلاحيات. الملاحظة المهمة أن **معاينة الفيديو المحلية كانت تعمل قبل الرفع الدائم**؛ لذلك بقي بند رفع الفيديو من الجهاز إلى التخزين الدائم وربطه بالشريحة مفتوحًا في سجل المشروع، رغم نجاح ربط فيديو Google Drive المرفوع يدويًا في الاختبار الأخير.

## 4. نظام شاشات العرض والتسويق

يعد مركز شاشات العرض من أكثر أجزاء المشروع توسعًا في الجولة الأخيرة. الميزات المنفذة تشمل إنشاء وإدارة الشاشات، روابط عامة وKiosk، Wake Lock عند توفره، مزامنة فورية، حملات خارجية، حملة Mix، شارات تسويقية، مدد وتأثيرات، نص عربي/إنجليزي، QR، شريط عروض، معاينة الصور والفيديو، وتخطيطات متعددة.

| الميزة | الحالة |
|---|---|
| عرض صورة واحدة | منفذة |
| عرض صورتين/3/4 متزامنة | منفذة ومحفوظة ضمن `displayLayout` |
| فيديو يسار وشرائح يمين | منفذة ومختبرة على شاشة 210001 |
| فصل دورة الفيديو عن الشرائح | منفذ؛ الفيديو لا يعاد تركيبه عند تبدل الصور |
| معاينة الصور | منفذة داخل لوحة التحكم |
| معاينة الفيديو قبل الحفظ | منفذة مع تشغيل وإيقاف وقراءة مدة |
| تكرار الفيديو | خيار `videoLoop` محفوظ لكل شريحة |
| عنوان ووصف فوق الصورة بلا بطاقة | منفذ حسب التصميم المطلوب |
| QR أبيض/أسود افتراضيًا مع تخصيص المطعم | منفذ |
| التاريخ والوقت في الشريط السفلي | منفذ |
| تدرج ووضوح الهوية العلوية | منفذ |
| Wake Lock واستعادة النشاط | منفذ ضمن حدود المتصفح |
| رفع فيديو محلي دائم من اللوحة | **مفتوح ويحتاج إكمالًا** |

آخر اختبار فعلي للرابط `tv/210001?kiosk=1` أثبت أن الفيديو أصبح يظهر بعد ربطه بتخزين NFOOD، لكن الربط اليدوي كان لازمًا لأن رابط Google Drive هو صفحة مشاركة وليس رابطًا مباشرًا دائمًا. لذلك يجب إنهاء مسار الرفع المباشر من لوحة التحكم قبل اعتماد التجربة كمسار إنتاجي كامل.

## 5. الاختبارات والتحقق

تم تنفيذ فحص TypeScript الحالي بنجاح:

```text
pnpm run check
CHECK_EXIT=0
```

كما تم تنفيذ مجموعة Vitest الكاملة بنجاح:

```text
Test Files  166 passed (166)
Tests       692 passed (692)
Duration    17.57s
```

النجاح هنا **نجاح برمجي آلي**، ولا يساوي وحده اختبارًا ميدانيًا على كل أنواع التلفزيونات والمتصفحات والكاميرات والأجهزة المحمولة. ما يزال اختبار Playwright متاحًا في المشروع، لكن بعض بنود الاختبار اليدوي الحقيقي مدرجة كمهام مفتوحة.

## 6. البنود المفتوحة ذات الأولوية

يحتوي `todo.md` حاليًا على **48 بندًا مفتوحًا**. ولأغراض التنفيذ، يمكن تجميعها في الأولويات التالية:

| الأولوية | البنود | الأثر |
|---|---|---|
| عاجلة | رفع الفيديو المحلي إلى التخزين الدائم، حفظ الرابط داخل الشريحة، إعادة اختبار 210001 | تمنع اعتماد تجربة الفيديو كمسار إنتاجي من لوحة التحكم |
| عالية | اختبار POS عند انقطاع الشبكة، اختبار صلاحيات السائق، اختبار نداء النادل والصوت | تتعلق بسلامة التشغيل الميداني |
| عالية | إكمال الترجمة الآلية الآمنة ومفاتيح المزودين وfallback | تؤثر في جودة المنتج متعدد اللغات |
| متوسطة | رفع صورة مخصصة للصنف، fallback للصور، التحقق من الروابط والصور المكسورة | تحسن جودة التسويق والعرض |
| متوسطة | حفظ سرعة وألوان وشفافية شريط الإعلان ورفع صوت مخصص أو مكتبة صوت | تكمل تخصيص مركز العرض |
| تحقق ميداني | QR بكاميرا هاتف حقيقية، NFC end-to-end، Profile Governance بجلسة أدمن، AI image generation عند توفر الرصيد | لا يمكن إثباتها آليًا بالكامل من بيئة الاختبارات الحالية |

## 7. المخاطر والقيود

أهم قيد تقني هو أن تشغيل الفيديو يعتمد على دعم المتصفح أو التلفاز للترميز الفعلي، وليس على امتداد الملف فقط. قبول رابط HTTPS أو ملف ذي MIME عام لا يضمن أن الجهاز يستطيع فك الترميز. يجب تفضيل MP4 بترميز H.264/AAC للتوافق الأوسع، مع استخدام WebM أو صيغ أخرى عند اختبار الجهاز المستهدف.

كما أن Wake Lock لا يمنح موقع الويب صلاحية منع إغلاق نظام التشغيل أو إيقاف الجهاز قسرًا. هو يطلب إبقاء الشاشة مضاءة عندما يسمح المتصفح، ويجب استخدام وضع Kiosk أو تطبيق جهاز مخصص في البيئات التجارية التي تتطلب منع الخروج من الشاشة.

كذلك، لا ينبغي اعتبار الاختبارات الآلية بديلًا عن اختبار POS والشاشات والتوصيل والكاميرات الفعلية. البنود اليدوية المفتوحة في `todo.md` هي قائمة قبول تشغيلية وليست بالضرورة أعطالًا مؤكدة.

## 8. توصية التنفيذ التالية

أوصي بتنفيذ الخطوات بالترتيب الآتي: أولًا إضافة رفع فيديو دائم من لوحة مركز العرض باستخدام `media.upload` و`storagePut` مع حفظ `mediaFileId` أو الرابط الدائم في الشريحة؛ ثانيًا اختبار شاشة 210001 في التخطيط الثنائي على المتصفح والتلفاز المستهدف؛ ثالثًا إجراء اختبار POS دون اتصال؛ رابعًا إغلاق صلاحيات السائق ونداء النادل في جلسات حقيقية؛ وخامسًا إكمال الترجمة الآلية الآمنة ومفاتيح المزودين.

بعد هذه الخطوات، يجب إجراء مراجعة قبول نهائية تشمل جهاز تلفاز فعليًا، متصفح Chromium، هاتفًا بكاميرا QR، جهاز POS، وحسابات أدمن ومطعم وسائق ونادل وعميل منفصلة.

## 9. الملفات والمراجع الداخلية

| الملف | الغرض |
|---|---|
| `client/src/pages/PublicDisplay.tsx` | شاشة العرض العامة، الفيديو، الصور، QR، التزامن، Kiosk |
| `client/src/components/RestaurantDisplayMarketingPanel.tsx` | لوحة إدارة الشاشات، الشرائح، الحملات والمعاينة |
| `client/src/lib/displayVideoSupport.ts` | خصائص وتشغيل الفيديو والتحقق المحلي |
| `server/routers.ts` | عقود الشاشات، الشرائح، الوسائط، الحفظ والمزامنة |
| `drizzle/schema.ts` | مخطط قاعدة البيانات وحقول الفيديو والتخطيط |
| `server/storage.ts` | رفع الملفات إلى التخزين الدائم |
| `client/src/locales/*.json` | قواميس الترجمة الموحدة |
| `todo.md` | سجل البنود المكتملة والمفتوحة |
| `package.json` | أوامر البناء والتحقق والاختبارات |

## المراجع

[^1]: سجل Git المحلي للمشروع، آخر checkpoint: `1fb59f7`، 2026-09-07.
[^2]: ملف `package.json` في مشروع `nfood-restaurant-saas`، أوامر `check` و`test` و`e2e` و`i18n`.

> هذا التقرير يصف حالة الكود والاختبارات كما ظهرت في بيئة المشروع وقت إنشائه، ويفصل بين الوظائف الموجودة فعليًا وبين الاختبارات الميدانية أو البنود التي ما تزال مفتوحة في `todo.md`.
